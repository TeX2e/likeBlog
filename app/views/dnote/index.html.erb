
<aside>
  <% if @navigate_tags %>
    <%= link_to 'Home', dnote_path %><br>
    <% @navigate_tags.each do |tag| %>
      └ <%= link_to tag, dnote_path(tag: tag) %><br>
    <% end %>
  <% end %>
</aside>

<main role="main">
  <div class="title">
    <h1>まこの独学ノート 一覧</h1>
    <hr>
  </div>

  <% @posts.reverse_each do |post| %>
    <div class="post-list">
      <span class="title">
        <%= link_to post.title, show_dnote_path(post) %>
      </span>
      <span class="tag">
        <%= " - #{post.tag}" if post.tag && post.tag != "" %>
      </span>
      <span class="date">
        <%= "#{post.date} " if post.date && post.date != "" %>
      </span>
      <hr>
    </div>
  <% end %>
</main>

